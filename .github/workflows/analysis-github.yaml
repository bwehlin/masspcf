name: Analysis (GitHub runners)
on:
  workflow_dispatch:
permissions:
  contents: write

jobs:
  analysis:
    uses: ./.github/workflows/analysis.yaml
    secrets: inherit
    with:
      use_github_runners: true
